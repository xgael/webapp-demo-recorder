# Copia este archivo a `.env` (ya está en .gitignore) y pon tu API key.
# El engine lo carga automáticamente al correr `recordDemo`.
# El shell siempre gana — si exportas la var, esto se ignora.

ELEVENLABS_API_KEY=
